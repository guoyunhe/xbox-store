# PlayStation Store

```bash
# initialize/update modules, add it to postinstall script of package.json
git submodule update --init --remote

# add a new module
git submodule add git@github.com:guoyunhe/nintendo-store.git src/modules/nintendo-store
```
